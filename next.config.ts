import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Development için bu ayarları kaldırıyoruz
  // output: "export",
  // trailingSlash: true,
  // basePath: "/nextjs-retro-console",
  // assetPrefix: "/nextjs-retro-console/",
};

export default nextConfig;
